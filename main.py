from calculation_functions.size_functions import calc_circle

my_diameter = float(input("Insert the diameter:\n"))

my_size, my_radius = calc_circle(double_up=True, diameter=10, rounding=5)

print(my_size)
pass

